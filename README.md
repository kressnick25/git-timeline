# Credentials
Crendentials can be loaded by environment variables. This is the recommended approach The following variables are requried:
- TOKEN_GITHUB

Crendentials can also be set through the credentials.yml file. See `credentials.example.yml`

# Port
Specify the port with a docker enviroment var: PORT